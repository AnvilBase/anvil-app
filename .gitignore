# Your own signing team and API keys. Copy Config/Local.xcconfig.example to get started,
# or run Scripts/bootstrap.sh.
Config/Local.xcconfig

# macOS and Xcode
.DS_Store
xcuserdata/
*.xcuserstate
*.xcscmblueprint
DerivedData/
build/
*.xcarchive
*.ipa
*.dSYM.zip

# Swift Package Manager working state (Package.resolved is committed on purpose)
.swiftpm/
.build/

# Model files are copied onto the phone separately and are far too large for git.
*.litertlm
*.gguf
*.task

# Queued tasks for claude-queue. Local scratch, and it names files after the task text, so anything
# pasted into /q — a key, a token — would otherwise land in the repository as a filename.
.claude-queue/

# Anvil's own system prompt, copied in by Scripts/bootstrap.sh from the private AnvilBase/anvil-prompt
# repository. Proprietary, so it never lands here; a build without it uses a short built-in default.
Sources/Prompt/DefaultPrompt.txt
Sources/Prompt/VoicePrompt.txt
